# 猫咪点击游戏 - 软件著作权设计说明文档

## 1. 软件总体设计

### 1.1 设计目标

实现一个基于 Canvas 的休闲点击游戏，支持抖音小游戏平台，具有以下特点：
- 原生 JavaScript (ES6+) 开发，无需第三方框架
- 流畅的 60fps 动画效果
- 多种运动模式和交互机制
- 完整的广告解锁和收益系统
- 优秀的视觉表现（多层草地渲染、粒子特效）

### 1.2 技术架构

**技术栈**：原生 JavaScript (ES6+) + Canvas 2D + 抖音小游戏 API (tt.*)

**架构模式**：分层管理器模式 + 中央协调器 + 组件化结构

```
用户输入 → InputManager → Game (中央协调器)
                              ├─ GameStateManager (状态管理)
                              ├─ SpawnManager (实体生成)
                              ├─ ResourceManager (资源加载)
                              ├─ AudioManager (音频管理)
                              ├─ AdManager (广告管理)
                              ├─ SettingsManager (设置持久化)
                              ├─ Renderers (背景/HUD/特效)
                              └─ Screens (开始/选择/结算)
```

### 1.3 系统总体架构图

```mermaid
graph TB
    subgraph "用户交互层"
        InputManager[InputManager<br/>输入管理器]
        Touch[触摸事件<br/>handleTouchStart/Move/End]
    end

    subgraph "界面层"
        StartScreen[StartScreen<br/>开始界面]
        SelectionScreen[SelectionScreen<br/>选择界面<br/>惯性滚动+粒子预览]
        GameOverScreen[GameOverScreen<br/>结算界面]
    end

    subgraph "渲染层"
        BackgroundRenderer[BackgroundRenderer<br/>背景渲染<br/>草地系统]
        HUDRenderer[HUDRenderer<br/>抬头显示]
        EffectsRenderer[EffectsRenderer<br/>特效渲染]
    end

    subgraph "核心协调层"
        Game[Game.js<br/>中央协调器<br/>主循环+事件分发]
    end

    subgraph "状态管理层"
        GameStateManager[GameStateManager<br/>状态管理<br/>无尽模式+解锁系统]
    end

    subgraph "业务管理层"
        SpawnManager[SpawnManager<br/>生成管理器]
        ResourceManager[ResourceManager<br/>资源管理器]
        AudioManager[AudioManager<br/>音频管理器<br/>三层架构]
        AdManager[AdManager<br/>广告管理器]
        EmojiManager[EmojiManager<br/>表情管理器]
        SidebarManager[SidebarManager<br/>侧边栏管理器]
    end

    subgraph "实体层"
        Entity[Entity<br/>基础实体]
        ImageTarget[ImageTarget<br/>图片目标<br/>12种运动模式]
        ParticleTarget[ParticleTarget<br/>粒子目标]
        BehaviorSystem[BehaviorSystem<br/>行为系统<br/>装死/叫声]
    end

    subgraph "工具层"
        Vector2[Vector2<br/>2D向量类]
        CanvasUtils[CanvasUtils<br/>画布工具]
        SettingsManager[SettingsManager<br/>设置持久化]
    end

    subgraph "草地渲染系统"
        GrassRenderer[GrassRenderer<br/>草地渲染器]
        WindSystem[WindSystem<br/>风系统]
        TouchInfluenceSystem[TouchInfluenceSystem<br/>触摸影响]
        GrassBlade[GrassBlade<br/>单个草叶]
    end

    Touch --> InputManager
    InputManager --> Game

    Game --> StartScreen
    Game --> SelectionScreen
    Game --> GameOverScreen

    Game --> BackgroundRenderer
    Game --> HUDRenderer
    Game --> EffectsRenderer

    Game --> GameStateManager
    Game --> SpawnManager
    Game --> ResourceManager
    Game --> AudioManager
    Game --> AdManager
    Game --> EmojiManager

    GameStateManager --> SpawnManager
    GameStateManager --> AdManager

    SpawnManager --> ImageTarget
    SpawnManager --> ParticleTarget

    ImageTarget --> Entity
    ImageTarget --> BehaviorSystem
    ParticleTarget --> ImageTarget

    BackgroundRenderer --> GrassRenderer
    GrassRenderer --> WindSystem
    GrassRenderer --> TouchInfluenceSystem
    GrassRenderer --> GrassBlade

    SelectionScreen --> ResourceManager
    SelectionScreen --> AdManager
    SelectionScreen --> EmojiManager

    Game --> SettingsManager
    ImageTarget --> Vector2

    style Game fill:#FFD700,stroke:#FFA500,stroke-width:3px
    style GameStateManager fill:#87CEEB,stroke:#4682B4,stroke-width:2px
    style ImageTarget fill:#98FB98,stroke:#3CB371,stroke-width:2px
    style GrassRenderer fill:#90EE90,stroke:#228B22,stroke-width:2px
    style AudioManager fill:#DDA0DD,stroke:#9370DB,stroke-width:2px
```

---

## 2. 软件结构图

### 2.1 目录结构

```
src/
├── Game.js                 # 中央协调器 (819行)
├── config.js               # 全局配置 (690行)
├── InputManager.js         # 输入管理
├── AudioManager.js         # 音频管理 (单例)
├── SettingsManager.js      # 设置持久化 (单例)
├── entities/
│   ├── Entity.js           # 基础实体
│   ├── ImageTarget.js      # 目标实体 (925行, 12种运动模式)
│   └── ParticleTarget.js   # 粒子目标
├── managers/
│   ├── GameStateManager.js # 状态管理
│   ├── SpawnManager.js     # 生成管理
│   ├── ResourceManager.js  # 资源管理
│   ├── AdManager.js        # 广告管理
│   ├── EmojiManager.js     # 表情管理
│   └── SidebarManager.js   # 侧边栏奖励
├── renderers/
│   ├── BackgroundRenderer.js
│   ├── HUDRenderer.js
│   ├── EffectsRenderer.js
│   └── grass/
│       ├── GrassRenderer.js
│       ├── WindSystem.js
│       ├── TouchInfluenceSystem.js
│       ├── GrassBlade.js
│       └── GrassDecoration.js
├── screens/
│   ├── StartScreen.js
│   ├── SelectionScreen.js
│   └── GameOverScreen.js
├── systems/
│   └── BehaviorSystem.js
├── behaviors/
│   ├── PlayDeadBehavior.js
│   └── VocalizationBehavior.js
└── utils/
    ├── Vector2.js
    └── CanvasUtils.js
```

### 2.2 模块依赖关系图

```mermaid
graph LR
    Game[Game.js] --> GameStateManager[GameStateManager]
    Game --> SpawnManager[SpawnManager]
    Game --> ResourceManager[ResourceManager]
    Game --> AudioManager[AudioManager]
    Game --> AdManager[AdManager]
    Game --> SettingsManager[SettingsManager]
    Game --> StartScreen[StartScreen]
    Game --> SelectionScreen[SelectionScreen]
    Game --> GameOverScreen[GameOverScreen]
    Game --> BackgroundRenderer[BackgroundRenderer]
    Game --> HUDRenderer[HUDRenderer]
    Game --> EffectsRenderer[EffectsRenderer]

    GameStateManager --> AdManager
    SelectionScreen --> ResourceManager
    SelectionScreen --> AdManager
    SpawnManager --> ImageTarget[ImageTarget]
    BackgroundRenderer --> GrassRenderer[GrassRenderer]
    ImageTarget --> BehaviorSystem[BehaviorSystem]

    style Game fill:#FFD700
    style GameStateManager fill:#87CEEB
    style ImageTarget fill:#98FB98
```

---

## 3. 功能流程图

### 3.1 游戏状态转换流程

```mermaid
graph LR
    START((开始界面<br/>START)) -->|选择目标| SELECT((选择界面<br/>SELECT))
    SELECT -->|选中卡片| PLAYING((游戏进行<br/>PLAYING))
    PLAYING -->|时间到| OVER((游戏结束<br/>OVER))
    OVER -->|再玩一次| SELECT

    PLAYING -->|设置按钮| SETTINGS((设置界面<br/>SETTINGS))
    SETTINGS -->|关闭| PLAYING

    SELECT -->|无尽模式| PLAYING_ENDLESS((无尽模式<br/>PLAYING))
    PLAYING_ENDLESS -->|主动退出| OVER

    PLAYING_ENDLESS -->|每500分| UNLOCK[解锁新目标]
    UNLOCK --> PLAYING_ENDLESS

    PLAYING_ENDLESS -->|每5秒| ATTRIBUTE[属性随机化<br/>speed/radius/points]
    ATTRIBUTE --> PLAYING_ENDLESS

    style START fill:#FFE4B5
    style SELECT fill:#E0BBE4
    style PLAYING fill:#957DAD
    style OVER fill:#D291BC
    style SETTINGS fill:#FEC8D8
    style PLAYING_ENDLESS fill:#FFDFD3
    style UNLOCK fill:#B5EAD7
    style ATTRIBUTE fill:#C7CEEA
```

### 3.2 触摸事件处理流程

```mermaid
graph TB
    Start([用户触摸屏幕]) --> InputMgr[InputManager<br/>捕获触摸事件]
    InputMgr --> CheckState{检查当前状态}

    CheckState -->|START状态| StartScreen[StartScreen<br/>处理开始按钮]
    CheckState -->|SELECT状态| SelectionScreen[SelectionScreen<br/>处理卡片滚动]
    CheckState -->|PLAYING状态| PlayingState[游戏进行中处理]
    CheckState -->|OVER状态| GameOverScreen[GameOverScreen<br/>处理重试按钮]

    StartScreen --> InitAudio[初始化音频系统<br/>AudioManager.init]
    InitAudio --> StartGame[调用Game.startGame<br/>切换到PLAYING状态]

    SelectionScreen --> HandleScroll{拖拽距离<br/>大于阈值?}
    HandleScroll -->|是| InertialScroll[惯性滚动<br/>velocity乘等于friction]
    HandleScroll -->|否| CheckCardClick{点击中间卡片?}
    InertialScroll --> SnapToCard[吸附到最近卡片]
    CheckCardClick -->|是| CheckUnlock{需要广告解锁?}
    CheckCardClick -->|否| SnapToCard
    CheckUnlock -->|是| ShowDialog[显示解锁确认弹窗]
    CheckUnlock -->|否| StartGame
    ShowDialog --> WatchAd[观看激励广告]
    WatchAd --> SaveUnlock[保存解锁数据]
    SaveUnlock --> StartGame

    PlayingState --> CheckTouch{触摸位置<br/>在目标内?}
    CheckTouch -->|是| CheckStartle{距离目标<br/>>150px?}
    CheckTouch -->|否| EmptyClick[空点击]
    CheckStartle -->|是| TriggerStartle[触发受惊机制<br/>ImageTarget.triggerStartle]
    CheckStartle -->|否| CatchTarget[捕获目标<br/>加分+播放音效]
    TriggerStartle --> FleeAnimation[逃离动画<br/>高速弹跳0.8秒]
    FleeAnimation --> SyncBasePoint[同步基准点<br/>syncMovementBasePoint]

    CatchTarget --> UpdateScore[GameStateManager<br/>addScore]
    UpdateScore --> CheckEndless{无尽模式?}
    CheckEndless -->|是| CheckUnlockScore{达到500分?}
    CheckEndless -->|否| CheckGameOver
    CheckUnlockScore -->|是| UnlockTarget[解锁新目标<br/>checkUnlock]
    CheckUnlockScore -->|否| CheckGameOver
    UnlockTarget --> RandomizeAttr[随机化属性<br/>每5秒]
    RandomizeAttr --> ContinueGame[继续游戏]
    CheckGameOver{时间到?} -->|是| EndGame[Game.endGame<br/>切换到OVER状态]
    CheckGameOver -->|否| ContinueGame

    EndGame --> ShowResult[显示结算界面]

    style InputMgr fill:#87CEEB
    style SelectionScreen fill:#DDA0DD
    style TriggerStartle fill:#FFB6C1
    style CatchTarget fill:#98FB98
    style StartGame fill:#FFD700
    style EndGame fill:#FFA07A
```

### 3.3 受惊机制执行流程

```mermaid
graph TB
    Start([目标更新帧]) --> CheckStartle{checkStartle<br/>距离小于150px?}

    CheckStartle -->|触发| SaveState[保存受惊前状态<br/>preStartleVelocity<br/>preStartleMovement]
    CheckStartle -->|未触发| NormalUpdate[正常运动更新<br/>12种运动模式之一]

    SaveState --> CalcFleeDir[计算逃离方向<br/>fleeDirection归一化]
    CalcFleeDir --> AddVariance[添加随机偏差<br/>正负25度角度范围]
    AddVariance --> SetFlags[设置受惊标志<br/>isStartled等于true<br/>startleTimer等于0.8秒]

    SetFlags --> StartleUpdate[受惊运动更新<br/>updateStartleMovement]

    StartleUpdate --> CalcSpeedFactor[计算速度渐变因子<br/>前20%加速到2.5倍<br/>后80%减速回正常]
    CalcSpeedFactor --> UpdateVelocity[更新速度向量<br/>velocity等于fleeDirection<br/>乘以速度乘以因子]
    UpdateVelocity --> MovePosition[移动位置<br/>position加等于<br/>velocity乘以dt]
    MovePosition --> CheckCollision[边界碰撞反弹<br/>velocity乘以负1]

    CheckCollision --> UpdateTimer[更新受惊计时器<br/>startleTimer减等于dt]
    UpdateTimer --> CheckEnd{startleTimer<br/>小于等于0?}

    CheckEnd -->|否| CalcSpeedFactor
    CheckEnd -->|是| RestoreState[恢复受惊前状态<br/>velocity等于<br/>preStartleVelocity]

    RestoreState --> SyncBasePoint[syncMovementBasePoint<br/>同步参数化运动基准点]

    SyncBasePoint --> CheckMode{运动模式?}

    CheckMode -->|wave| SyncWave[baseY等于<br/>pos.y减sin时间乘50]
    CheckMode -->|hover| SyncHover[baseX等于pos.x减hoverX<br/>baseY等于pos.y减hoverY]
    CheckMode -->|pendulum| SyncPendulum[pivotX等于pos.x减<br/>length乘以sin角度]
    CheckMode -->|circular| SyncCircular[orbitCenter等于pos减<br/>radius乘以cos除sin相位]
    CheckMode -->|spiral| SyncSpiral[spiralCenter等于pos减<br/>spiralRadius乘以cos除sin]
    CheckMode -->|zigzag| SyncZigzag[baseY等于pos.y减<br/>amplitude乘以三角波]
    CheckMode -->|figure8| SyncFigure8[center等于pos减<br/>amplitude乘以sin]
    CheckMode -->|bounce/random/dash/chase| NoSync[无需同步<br/>速度累积模式]

    SyncWave --> End([返回正常更新])
    SyncHover --> End
    SyncPendulum --> End
    SyncCircular --> End
    SyncSpiral --> End
    SyncZigzag --> End
    SyncFigure8 --> End
    NoSync --> End

    NormalUpdate --> End

    style SaveState fill:#FFE4E1
    style CalcFleeDir fill:#E0BBE4
    style StartleUpdate fill:#FFB6C1
    style SyncBasePoint fill:#98FB98
    style End fill:#FFD700
```

### 3.4 无尽模式流程

```mermaid
graph TB
    Start([选择无尽模式]) --> GetAvailable[getAvailableTargetIndices<br/>获取可用目标列表]

    GetAvailable --> CheckConfig{USE_ALL_TARGETS<br/>配置?}

    CheckConfig -->|true| AllTargets[返回所有目标索引<br/>0到N-1]
    CheckConfig -->|false| CheckUnlock[检查AdManager<br/>isTargetUnlocked]

    CheckUnlock --> UnlockedIndices[返回已解锁目标索引]
    AllTargets --> SelectRandom
    UnlockedIndices --> SelectRandom

    SelectRandom[随机选择初始目标] --> InitState[初始化无尽模式状态<br/>unlockedTargetIndices<br/>nextUnlockScore等于500]

    InitState --> StartGame[startGame<br/>切换到PLAYING状态]

    StartGame --> GameLoop[游戏主循环]

    GameLoop --> UpdateTimer[更新计时器<br/>gameTimer加等于dt]
    UpdateTimer --> CheckAttrChange{计时器大于等于5秒?}

    CheckAttrChange -->|是| RandomizeAttr[randomizeAttributes<br/>随机化乘数]
    CheckAttrChange -->|否| CheckScore

    RandomizeAttr[速度乘数:0.8-1.5<br/>半径乘数:0.7-1.3<br/>分数乘数:0.5-2.0] --> ResetTimer[重置计时器]
    ResetTimer --> CheckScore

    CheckScore{分数大于等于<br/>nextUnlockScore?} -->|是| GetAvailable2[重新获取可用目标]
    CheckScore -->|否| ContinueGame[继续游戏]

    GetAvailable2 --> FindNext[查找下一个<br/>未解锁目标]
    FindNext --> Found{找到?}

    Found -->|是| AddToUnlocked[添加到解锁列表<br/>unlockedTargetIndices.push]
    Found -->|否| AllUnlocked[所有目标已解锁]

    AddToUnlocked --> ShowNotify[显示解锁通知<br/>unlockNotification]
    ShowNotify --> UpdateNextScore[nextUnlockScore加等于500]
    UpdateNextScore --> CheckAdTrigger{应该触发<br/>解锁广告?}

    CheckAdTrigger -->|是| TriggerAd[异步触发广告<br/>50%概率]
    CheckAdTrigger -->|否| ContinueGame
    TriggerAd --> ContinueGame

    AllUnlocked --> ContinueGame
    ContinueGame --> GameLoop

    style Start fill:#FFD700
    style InitState fill:#98FB98
    style RandomizeAttr fill:#DDA0DD
    style AddToUnlocked fill:#87CEEB
    style ContinueGame fill:#90EE90
```

### 3.5 广告解锁流程

```mermaid
graph TB
    Start([用户点击锁定卡片]) --> ShowDialog[显示解锁确认弹窗<br/>showUnlockDialog等于true]

    ShowDialog --> UserClick{用户点击}

    UserClick -->|确认按钮| CheckAdManager{AdManager<br/>存在?}
    UserClick -->|取消按钮| CloseDialog[关闭弹窗<br/>showUnlockDialog等于false]
    UserClick -->|弹窗外部| CloseDialog

    CheckAdManager -->|否| LogError[记录错误日志]
    CheckAdManager -->|是| RequestUnlock[AdManager.requestUnlock]

    RequestUnlock --> ShowRewardedAd[tt.createRewardedVideoAd<br/>展示激励视频广告]

    ShowRewardedAd --> AdResult{广告结果}

    AdResult -->|观看完成| GetUnlockData[SettingsManager.getUnlockData<br/>获取现有解锁数据]
    AdResult -->|跳过/失败| LogAdFail[记录广告失败]

    GetUnlockData --> CheckExisting{已有解锁数据?}

    CheckExisting -->|是| UpdateData[更新解锁时间戳<br/>unlockTime等于Date.now]
    CheckExisting -->|否| CreateData[创建新解锁记录<br/>unlockTime等于Date.now]

    UpdateData --> SaveUnlock[SettingsManager.saveUnlockData<br/>保存到本地存储]
    CreateData --> SaveUnlock

    SaveUnlock --> CheckDuration{unlockDuration<br/>有效期?}

    CheckDuration -->|负1永久| Permanent[永久解锁<br/>expireTime等于null]
    CheckDuration -->|0免费| FreeTarget[免费目标<br/>expireTime等于null]
    CheckDuration -->|大于0限时| Timed[计算过期时间<br/>expireTime等于unlockTime加duration]

    Permanent --> UnlockSuccess[解锁成功<br/>isTargetUnlocked等于true]
    FreeTarget --> UnlockSuccess
    Timed --> UnlockSuccess

    UnlockSuccess --> CloseSuccessDialog[关闭弹窗<br/>返回选择界面]

    CloseSuccessDialog --> UpdateUI[更新卡片UI<br/>移除锁图标<br/>显示剩余时间]

    LogAdFail --> CloseFailDialog[关闭弹窗]

    LogError --> CloseFailDialog
    CloseDialog --> End([结束])
    CloseSuccessDialog --> End
    CloseFailDialog --> End
    UpdateUI --> End

    style ShowDialog fill:#FFE4B5
    style ShowRewardedAd fill:#FFB6C1
    style SaveUnlock fill:#98FB98
    style UnlockSuccess fill:#87CEEB
    style UpdateUI fill:#DDA0DD
```

### 3.6 音频分层播放流程

```mermaid
graph TB
    subgraph "音频分层架构"
        BGM[BGM层<br/>背景音乐]
        GAME_SFX[GAME_SFX层<br/>游戏音效<br/>点击/倒计时/结束]
        TARGET_SFX[TARGET_SFX层<br/>目标音效<br/>受惊/捕获]
    end

    subgraph "音频类型"
        Click[点击音效<br/>click.mp3]
        Countdown[倒计时音效<br/>countdown.mp3]
        GameOver[游戏结束音效<br/>game_over.mp3]
        Startle[受惊音效<br/>startle.mp3]
        Catch[捕获音效<br/>catch.mp3]
        PlayDead[装死音效<br/>play_dead.mp3]
    end

    subgraph "控制参数"
        MasterVolume[主音量<br/>masterVolume]
        LayerVolume[层音量<br/>bgmVolume<br/>gameSfxVolume<br/>targetSfxVolume]
    end

    subgraph "播放逻辑"
        Init[初始化<br/>tt.createInnerAudioContext]
        Load[加载音频<br/>audio.src]
        Play[播放<br/>audio.play]
        Stop[停止<br/>audio.stop]
        Loop[循环<br/>audio.loop]
    end

    BGM --> LayerVolume
    GAME_SFX --> LayerVolume
    TARGET_SFX --> LayerVolume

    LayerVolume --> MasterVolume

    Click --> GAME_SFX
    Countdown --> GAME_SFX
    GameOver --> GAME_SFX

    Startle --> TARGET_SFX
    Catch --> TARGET_SFX
    PlayDead --> TARGET_SFX

    GAME_SFX --> Init
    TARGET_SFX --> Init

    Init --> Load
    Load --> Play

    Play --> Stop
    Play --> Loop

    style BGM fill:#FFB6C1
    style GAME_SFX fill:#98FB98
    style TARGET_SFX fill:#87CEEB
    style MasterVolume fill:#FFD700
```

### 3.7 多层草地渲染流程

```mermaid
graph TB
    subgraph "草地渲染系统"
        GrassRenderer[GrassRenderer<br/>草地渲染器]

        subgraph "核心子系统"
            WindSystem[WindSystem<br/>风系统]
            TouchSystem[TouchInfluenceSystem<br/>触摸影响系统]
        end

        subgraph "渲染组件"
            GrassBlade[GrassBlade<br/>单个草叶]
            Decoration[GrassDecoration<br/>装饰元素]
        end

        subgraph "LOD系统"
            LOD[LOD细节层次<br/>high/medium/low]
            FPS[fpsHistory<br/>帧率历史]
        end
    end

    subgraph "三层草地结构"
        Foreground[前景层foreground<br/>大草叶12-20px<br/>密度5 透明度1.0]
        Midground[中景层midground<br/>中等8-14px<br/>密度8 透明度0.8]
        Background[远景层background<br/>小草5-10px<br/>密度12 透明度0.6]
    end

    GrassRenderer --> WindSystem
    GrassRenderer --> TouchSystem
    GrassRenderer --> GrassBlade
    GrassRenderer --> Decoration
    GrassRenderer --> LOD

    GrassRenderer --> Foreground
    GrassRenderer --> Midground
    GrassRenderer --> Background

    WindSystem --> UpdateWind[更新风向向量<br/>基础风向+阵风]

    TouchSystem --> AddTouch[添加触摸影响<br/>距离衰减]

    GrassBlade --> DrawBlade[绘制单个草叶<br/>贝塞尔曲线]

    Decoration --> DrawFlower[绘制花朵]
    Decoration --> DrawStone[绘制石头]

    LOD --> CheckFPS[检查帧率<br/>每0.5秒]
    CheckFPS --> GetSkipRatio[获取跳过比例<br/>high:0 medium:0.2 low:0.5]

    GetSkipRatio --> UpdateLayer[更新层级<br/>随机跳过部分草叶]

    Foreground --> UpdateLayer
    Midground --> UpdateLayer
    Background --> UpdateLayer

    UpdateLayer --> RenderOrder[渲染顺序<br/>远景->中景->前景]

    style GrassRenderer fill:#FFD700
    style WindSystem fill:#87CEEB
    style TouchSystem fill:#DDA0DD
    style Foreground fill:#98FB98
    style Midground fill:#90EE90
    style Background fill:#7CCD7C
    style LOD fill:#FFB6C1
```

---

## 4. 逻辑框图

### 4.1 运动模式分类

```mermaid
graph TB
    subgraph "速度累积模式<br/>position加等于velocity乘以dt"
        BOUNCE[bounce<br/>边界弹跳<br/>velocity乘以负0.8]
        RANDOM[random<br/>随机方向变化<br/>定时改变velocity]
        DASH[dash<br/>冲刺停顿<br/>高速冲刺加暂停]
        CHASE[chase<br/>追逐虚拟目标<br/>velocity指向移动目标]
    end

    subgraph "参数化模式<br/>position等于基准点加动态偏移"
        WAVE[wave<br/>波浪运动<br/>y等于baseY加sin时间乘50]
        HOVER[hover<br/>悬停飘动<br/>多频率正弦波叠加]
        PENDULUM[pendulum<br/>钟摆运动<br/>绕支点摆动]
        CIRCULAR[circular<br/>圆周运动<br/>绕固定中心]
        SPIRAL[spiral<br/>螺旋运动<br/>半径周期变化]
        ZIGZAG[zigzag<br/>锯齿运动<br/>三角波垂直]
        FIGURE8[figure8<br/>8字形运动<br/>李萨如曲线]
    end

    BOUNCE --> UseVelocity[使用velocity更新位置]
    RANDOM --> UseVelocity
    DASH --> UseVelocity
    CHASE --> UseVelocity

    WAVE --> UseBase[使用基准点加偏移]
    HOVER --> UseBase
    PENDULUM --> UseBase
    CIRCULAR --> UseBase
    SPIRAL --> UseBase
    ZIGZAG --> UseBase
    FIGURE8 --> UseBase

    UseBase --> SyncNeeded[受惊后需要<br/>syncMovementBasePoint]

    style BOUNCE fill:#FFE4B5
    style WAVE fill:#E0BBE4
    style UseVelocity fill:#98FB98
    style UseBase fill:#87CEEB
    style SyncNeeded fill:#FFB6C1
```

### 4.2 惯性滚动算法流程

```mermaid
graph TB
    Start([用户拖动卡片]) --> HandleMove[handleTouchMove<br/>更新scrollOffset]

    HandleMove --> CalcVelocity[计算滚动速度<br/>velocity等于lastX减x<br/>除以dt乘16]

    CalcVelocity --> LimitVelocity[限制最大速度<br/>maxVelocity等于25]

    LimitVelocity --> UpdateOffset[scrollOffset加等于velocity]

    UpdateOffset --> CheckBoundary{超出边界?}

    CheckBoundary -->|是| AddFriction[增加阻尼<br/>friction等于0.7]
    CheckBoundary -->|否| NormalFriction[正常阻尼<br/>friction等于0.95]

    AddFriction --> ApplyFriction[velocity乘等于friction]
    NormalFriction --> ApplyFriction

    ApplyFriction --> CheckVelocity{velocity绝对值小于1?}

    CheckVelocity -->|否| ContinueInertia[继续惯性滚动]
    CheckVelocity -->|是| StartSnapping[开始吸附]

    ContinueInertia --> HardLimit{硬边界限制}
    HardLimit -->|超出| SetVelocityZero[velocity等于0<br/>限制offset]
    HardLimit -->|未超出| UpdateOffset

    StartSnapping --> CalcTargetIndex[计算目标索引<br/>targetIndex等于round<br/>offset除以cardStep]

    CalcTargetIndex --> ClampIndex[限制索引范围<br/>0到length减1]

    ClampIndex --> CalcTargetOffset[计算目标偏移<br/>targetOffset等于<br/>index乘以cardStep]

    CalcTargetOffset --> SmoothSnap[平滑吸附<br/>offset加等于<br/>target减offset乘0.15]

    SmoothSnap --> CheckClose{距离小于0.5?}

    CheckClose -->|否| SmoothSnap
    CheckClose -->|是| SetOffset[设置最终偏移<br/>offset等于targetOffset]

    SetOffset --> CheckBoundary2{超出边界?}

    CheckBoundary2 -->|是| ElasticReturn[弹性回弹<br/>offset加等于<br/>0减offset乘0.2]
    CheckBoundary2 -->|否| AutoScrollCheck{自动轮播?}

    ElasticReturn --> AutoScrollCheck

    AutoScrollCheck -->|计时器到| NextCard[移动到下一张卡片<br/>currentIndex加加]
    AutoScrollCheck -->|未到| End([结束])

    NextCard --> HandleLast{最后一张?}
    HandleLast -->|是| WrapToFirst[循环到第一张<br/>velocity等于负offset除10]
    HandleLast -->|否| SetSnapping[isSnapping等于true]

    WrapToFirst --> SetSnapping
    SetSnapping --> End

    style HandleMove fill:#FFE4B5
    style ApplyFriction fill:#E0BBE4
    style SmoothSnap fill:#98FB98
    style AutoScrollCheck fill:#87CEEB
```

### 4.3 类关系图

```mermaid
graph TB
    subgraph "实体继承关系"
        Entity[Entity<br/>基础实体类<br/>position radius]
    end

    subgraph "目标类型"
        ImageTarget[ImageTarget<br/>图片目标<br/>12种运动模式]
        ParticleTarget[ParticleTarget<br/>粒子目标<br/>继承ImageTarget]
    end

    subgraph "行为系统"
        BehaviorSystem[BehaviorSystem<br/>行为管理器]
        PlayDead[PlayDeadBehavior<br/>装死行为]
        Vocalization[VocalizationBehavior<br/>叫声行为]
    end

    subgraph "运动模式"
        VelocityBased[速度累积模式<br/>bounce/random/dash/chase]
        Parametric[参数化模式<br/>wave/hover/pendulum<br/>circular/spiral/zigzag/figure8]
    end

    subgraph "受惊机制"
        Startle[受惊状态<br/>isStartled]
        Flee[逃离方向<br/>fleeDirection]
        Sync[同步基准点<br/>syncMovementBasePoint]
    end

    subgraph "渲染"
        Render[render方法<br/>旋转缩放]
        Exclamation[惊叹号特效<br/>renderExclamation]
        Expression[行为表情<br/>renderBehaviors]
    end

    Entity --> ImageTarget
    ImageTarget --> ParticleTarget

    ImageTarget --> BehaviorSystem

    BehaviorSystem --> PlayDead
    BehaviorSystem --> Vocalization

    ImageTarget --> VelocityBased
    ImageTarget --> Parametric

    ImageTarget --> Startle
    Startle --> Flee
    Startle --> Sync

    ImageTarget --> Render
    Render --> Exclamation
    Render --> Expression

    style Entity fill:#FFE4B5
    style ImageTarget fill:#87CEEB
    style ParticleTarget fill:#DDA0DD
    style BehaviorSystem fill:#98FB98
    style Startle fill:#FFB6C1
```

---

## 5. 接口设计

### 5.1 模块间接口定义

#### Game.js 公共 API

```javascript
class Game {
    // 初始化游戏
    constructor(canvas)

    // 游戏流程控制
    start()                    // 启动游戏主循环
    startGame(targetConfig)    // 开始游戏
    startEndlessMode()         // 启动无尽模式
    endGame()                  // 结束游戏

    // 触摸事件处理
    handleTouchStart(pos)      // 触摸开始
    handleTouchMove(pos)       // 触摸移动
    handleTouchEnd(pos)        // 触摸结束

    // 渲染
    render()                   // 渲染游戏画面
}
```

#### GameStateManager 接口

```javascript
class GameStateManager {
    // 状态管理
    setState(newState)         // 设置状态
    getState()                 // 获取当前状态
    enterSettings()            // 进入设置
    exitSettings()             // 退出设置

    // 游戏流程
    startGame(target, isEndless) // 开始游戏
    startEndlessMode()         // 启动无尽模式
    endGame()                  // 结束游戏

    // 分数系统
    addScore(points)           // 增加分数
    getCurrentTargetId()       // 获取当前目标ID

    // 无尽模式
    checkUnlock()              // 检查解锁新目标
    randomizeAttributes()      // 随机化属性
    getAvailableTargetIndices() // 获取可用目标索引

    // 游戏逻辑
    update(dt)                 // 更新游戏逻辑
}
```

#### ImageTarget 接口

```javascript
class ImageTarget extends Entity {
    // 运动更新
    update(dt, canvasWidth, canvasHeight)

    // 受惊机制
    checkStartle(touchPosition)    // 检查受惊
    triggerStartle(touchPosition)  // 触发受惊
    syncMovementBasePoint()        // 同步基准点

    // 渲染
    render(ctx)                    // 渲染目标

    // 运动模式（内部方法）
    updateBounce(dt, w, h)
    updateWave(dt, w, h)
    updateRandom(dt, w, h)
    updateCircular(dt, w, h)
    updateSpiral(dt, w, h)
    updateZigzag(dt, w, h)
    updateFigure8(dt, w, h)
    updateDash(dt, w, h)
    updateHover(dt, w, h)
    updatePendulum(dt, w, h)
    updateChase(dt, w, h)
}
```

### 5.2 事件回调接口

```javascript
// InputManager 回调函数类型
type TouchCallback = (position: Vector2) => void

interface InputManager {
    onTouchStart: TouchCallback
    onTouchMove: TouchCallback
    onTouchEnd: TouchCallback
}

// AdManager 回调函数类型
type AdResultCallback = (success: boolean) => void

interface AdManager {
    requestUnlock(targetId: string): Promise<boolean>
    isTargetUnlocked(targetId: string): boolean
}
```

### 5.3 抖音 API 集成接口

```javascript
// 图片加载
tt.createImage(): HTMLImageElement

// 音频上下文
tt.createInnerAudioContext(): InnerAudioContext

// 广告API
tt.createRewardedVideoAd(options): RewardedVideoAd

// 本地存储
tt.setStorageSync(key: string, data: any): void
tt.getStorageSync(key: string): any

// 系统信息
tt.getSystemInfoSync(): SystemInfo
```

---

## 6. 模块名称功能

### 6.1 核心模块

| 模块名称 | 文件路径 | 行数 | 功能描述 |
|---------|---------|------|---------|
| Game | src/Game.js | 819 | 中央协调器，管理游戏主循环、状态转换、事件分发 |
| config | src/config.js | 690 | 全局配置，包括目标类型、运动参数、UI配置 |

### 6.2 管理器模块

| 模块名称 | 文件路径 | 行数 | 功能描述 |
|---------|---------|------|---------|
| GameStateManager | src/managers/GameStateManager.js | 370 | 状态机管理、无尽模式逻辑、解锁系统 |
| SpawnManager | src/managers/SpawnManager.js | - | 目标生成工厂，控制生成频率和数量 |
| ResourceManager | src/managers/ResourceManager.js | - | 资源预加载和缓存管理 |
| AudioManager | src/managers/AudioManager.js | 250 | 三层音频架构（BGM/GAME_SFX/TARGET_SFX） |
| AdManager | src/managers/AdManager.js | 400 | 广告触发、解锁数据管理、时效检查 |
| EmojiManager | src/managers/EmojiManager.js | - | Emoji精灵图渲染管理 |
| SidebarManager | src/managers/SidebarManager.js | - | 侧边栏入口和奖励动画 |

### 6.3 渲染模块

| 模块名称 | 文件路径 | 功能描述 |
|---------|---------|---------|
| BackgroundRenderer | src/renderers/BackgroundRenderer.js | 背景渲染，集成草地系统 |
| HUDRenderer | src/renderers/HUDRenderer.js | 抬头显示（分数、倒计时） |
| EffectsRenderer | src/renderers/EffectsRenderer.js | 特效渲染（粒子、爆炸） |
| GrassRenderer | src/renderers/grass/GrassRenderer.js | 多层草地渲染系统（420行） |
| WindSystem | src/renderers/grass/WindSystem.js | 风力计算和阵风模拟 |
| TouchInfluenceSystem | src/renderers/grass/TouchInfluenceSystem.js | 触摸交互影响系统 |
| GrassBlade | src/renderers/grass/GrassBlade.js | 单个草叶对象，贝塞尔曲线渲染 |
| GrassDecoration | src/renderers/grass/GrassDecoration.js | 草地装饰元素（花、石头） |

### 6.4 实体模块

| 模块名称 | 文件路径 | 行数 | 功能描述 |
|---------|---------|------|---------|
| Entity | src/entities/Entity.js | - | 基础实体类 |
| ImageTarget | src/entities/ImageTarget.js | 987 | 图片目标，12种运动模式+受惊机制 |
| ParticleTarget | src/entities/ParticleTarget.js | - | 粒子目标，继承ImageTarget |

### 6.5 界面模块

| 模块名称 | 文件路径 | 行数 | 功能描述 |
|---------|---------|------|---------|
| StartScreen | src/screens/StartScreen.js | - | 开始界面 |
| SelectionScreen | src/screens/SelectionScreen.js | 1023 | 卡片选择，惯性滚动+粒子预览+解锁弹窗 |
| GameOverScreen | src/screens/GameOverScreen.js | - | 结算界面 |

---

## 7. 函数名称功能

### 7.1 Game.js 核心函数

| 函数名 | 功能描述 |
|--------|---------|
| `constructor(canvas)` | 初始化所有管理器和组件 |
| `start()` | 启动游戏主循环（requestAnimationFrame） |
| `update(dt)` | 每帧更新逻辑（状态管理器→目标→屏幕） |
| `render()` | 每帧渲染（背景→目标→HUD→屏幕→特效） |
| `handleTouchStart(pos)` | 处理触摸开始，分发到当前屏幕 |
| `handleTouchMove(pos)` | 处理触摸移动 |
| `handleTouchEnd(pos)` | 处理触摸结束 |
| `startGame(targetConfig)` | 开始游戏，初始化状态和目标 |
| `startEndlessMode()` | 启动无尽模式 |
| `endGame()` | 结束游戏，保存最高分 |

### 7.2 ImageTarget 运动函数

| 函数名 | 功能描述 |
|--------|---------|
| `update(dt, w, h)` | 主更新循环，选择运动模式 |
| `render(ctx)` | Canvas渲染，旋转+缩放+受惊特效 |
| `checkStartle(pos)` | 受惊检测，距离<150px触发 |
| `triggerStartle(pos)` | 触发受惊，计算逃离方向 |
| `syncMovementBasePoint()` | 同步参数化运动基准点 |
| `updateBounce()` | 弹跳运动 |
| `updateWave()` | 波浪运动 |
| `updateRandom()` | 随机方向变化 |
| `updateCircular()` | 圆周运动 |
| `updateSpiral()` | 螺旋运动 |
| `updateZigzag()` | 锯齿运动 |
| `updateFigure8()` | 8字形运动 |
| `updateDash()` | 冲刺停顿 |
| `updateHover()` | 悬停飘动 |
| `updatePendulum()` | 钟摆运动 |
| `updateChase()` | 追逐虚拟目标 |

### 7.3 GameStateManager 状态函数

| 函数名 | 功能描述 |
|--------|---------|
| `setState(newState)` | 设置游戏状态 |
| `getState()` | 获取当前状态 |
| `enterSettings()` | 进入设置界面 |
| `exitSettings()` | 退出设置界面 |
| `startGame(target, isEndless)` | 开始游戏 |
| `startEndlessMode()` | 启动无尽模式 |
| `endGame()` | 结束游戏 |
| `addScore(points)` | 增加分数 |
| `checkUnlock()` | 检查解锁新目标 |
| `randomizeAttributes()` | 随机化无尽模式属性 |
| `getAvailableTargetIndices()` | 获取可用目标索引 |
| `update(dt)` | 更新游戏逻辑 |

### 7.4 SelectionScreen 滚动函数

| 函数名 | 功能描述 |
|--------|---------|
| `handleTouchStart(pos)` | 触摸开始，记录拖动起点 |
| `handleTouchMove(pos)` | 触摸移动，更新偏移+计算速度 |
| `handleTouchEnd(pos)` | 触摸结束，判断点击/滑动 |
| `update(dt)` | 惯性滚动+吸附+自动轮播 |
| `renderCard(index, x, y)` | 渲染单个卡片 |
| `renderParticlePreview()` | 渲染粒子预览效果 |
| `handleCardClick(pos)` | 处理卡片点击 |
| `handleUnlockDialogClick(pos)` | 处理解锁弹窗点击 |
| `requestUnlockTarget()` | 请求解锁目标 |
| `selectTargetById(id)` | 根据ID选中卡片 |

---

## 8. 算法说明

### 8.1 运动算法详解

#### 速度累积模式（4种）

**bounce（弹跳）**：
```
position += velocity * dt
if 超出边界:
    position = 边界位置
    velocity *= -0.8  // 反弹并衰减
```

**random（随机方向）**：
```
if Math.random() < 0.02:
    angle = Math.random() * 2π
    velocity = (cos(angle), sin(angle)) * speed
position += velocity * dt
```

**dash（冲刺）**：
```
if isDashing:
    position += dashVelocity * dt  // 高速冲刺
else:
    position += wobble  // 轻微晃动
if timer > 阈值:
    切换冲刺/停顿状态
```

**chase（追逐）**：
```
targetPos = (centerX + radius*sin(t), centerY + radius*cos(t))
direction = normalize(targetPos - position)
position += direction * chaseSpeed * dt
```

#### 参数化模式（8种）

**wave（波浪）**：
```
position.x += direction * speed * dt
position.y = baseY + sin(time) * 50
```

**hover（悬停）**：
```
baseX += driftDirection * driftSpeed * dt
hoverX = amplitude * sin(time*1.5) + 0.5*amplitude * sin(time*2.7)
hoverY = amplitude * sin(time*1.8) + 0.5*amplitude * sin(time*3.1)
position = (baseX + hoverX, baseY + hoverY)
```

**pendulum（钟摆）**：
```
angle = maxAngle * sin(time * angularFreq)
position.x = pivotX + length * sin(angle)
position.y = pivotY + length * cos(angle)
```

**circular（圆周）**：
```
position.x = orbitCenterX + orbitRadius * cos(time*angularSpeed + phase)
position.y = orbitCenterY + orbitRadius * sin(time*angularSpeed + phase)
```

**spiral（螺旋）**：
```
spiralRadius 在 baseRadius 和 maxRadius 之间周期性变化
position.x = spiralCenterX + spiralRadius * cos(time*angularSpeed + phase)
position.y = spiralCenterY + spiralRadius * sin(time*angularSpeed + phase)
```

**zigzag（锯齿）**：
```
position.x += direction * speed * dt
t = (time * frequency) % 1
triangle = t < 0.5 ? 4*t - 1 : 3 - 4*t
position.y = baseY + amplitude * triangle
```

**figure8（8字形）**：
```
position.x = center.x + amplitudeX * sin(2 * time * angularSpeed)
position.y = center.y + amplitudeY * sin(time * angularSpeed)
```

### 8.2 受惊机制算法

```
触发条件：distance < 150px
执行流程：
1. 保存 preStartleVelocity 和 preStartleMovement
2. 计算 fleeDirection = normalize(touchPos - position)
3. 添加随机偏差 ±25度
4. 速度渐变：
   - 前20%：加速到 2.5倍
   - 后80%：减速回正常
5. 受惊结束时调用 syncMovementBasePoint() 同步基准点
```

**基准点同步公式**（以 hover 为例）：
```
baseX = position.x - hoverX(time)
baseY = position.y - hoverY(time)
其中：
hoverX = amp * sin(time*1.5) + 0.5*amp * sin(time*2.7)
hoverY = amp * sin(time*1.8) + 0.5*amp * sin(time*3.1)
```

### 8.3 无尽模式算法

```
初始化：
1. 根据 USE_ALL_TARGETS 配置确定目标池
2. 随机选择初始目标
3. 设置 nextUnlockScore = 500

游戏循环：
1. 每5秒随机化属性：
   - speedMultiplier: [0.8, 1.5]
   - radiusMultiplier: [0.7, 1.3]
   - pointsMultiplier: [0.5, 2.0]
2. 每500分解锁新目标：
   - 从目标池中选择下一个未解锁目标
   - 添加到 unlockedTargetIndices
   - nextUnlockScore += 500
```

### 8.4 草地渲染算法

**三层LOD系统**：
```
前景层: bladeHeight [12, 20], density 5, opacity 1.0
中景层: bladeHeight [8, 14], density 8, opacity 0.8
远景层: bladeHeight [5, 10], density 12, opacity 0.6
```

**风力算法**：
```
gustPhase = (timer % gustDuration) / gustDuration  // 0-1 循环
gustFactor = sin(gustPhase * π)  // 半正弦波
currentIntensity = 1 + gustFactor * gustIntensity

angleVariance = sin(time*0.001) * 0.15 + sin(time*0.0007) * 0.05
finalAngle = baseAngle + angleVariance
windVector = (cos(finalAngle)*intensity, sin(finalAngle)*intensity)
```

**LOD动态调整**：
```
if lodLevel == 'high': skipRatio = 0
if lodLevel == 'medium': skipRatio = 0.2
if lodLevel == 'low': skipRatio = 0.5

更新/渲染时随机跳过部分草叶
```

---

## 9. 运行设计

### 9.1 运行环境

- **平台**：抖音小游戏平台
- **引擎**：原生 JavaScript (ES6+) + Canvas 2D
- **设备支持**：iOS/Android 移动设备
- **DPR支持**：高DPI设备适配

### 9.2 数据流

```
用户触摸
  ↓
InputManager (捕获+标准化)
  ↓
Game (事件分发)
  ↓
┌─────────┬─────────┬─────────┬─────────┐
↓         ↓         ↓         ↓         ↓
StartScreen  SelectionScreen  Playing  GameOverScreen
              ↓              ↓
          惯性滚动      捕获目标
          粒子预览      触发受惊
          解锁弹窗      更新分数
```

### 9.3 错误处理

| 错误类型 | 处理方式 |
|---------|---------|
| 资源加载失败 | 显示占位符，记录日志 |
| 音频初始化失败 | 延迟到用户交互后初始化 |
| 广告加载失败 | 降级为免费体验，记录日志 |
| 存储空间不足 | 清理过期数据 |

### 9.4 性能优化

1. **OffscreenCanvas 缓存**：静态元素预渲染
2. **LOD系统**：根据帧率动态调整渲染质量
3. **对象池**：复用粒子和特效对象
4. **分层渲染**：远景低密度，前景高密度

---

## 10. 创新点说明

### 10.1 多层草地渲染系统

**技术特点**：
- 三层LOD系统（前景/中景/远景）
- 贝塞尔曲线绘制自然草叶形状
- 风力系统模拟自然风（阵风+方向摆动）
- 触摸交互影响（多点触摸+距离衰减）

### 10.2 音频分层架构

**技术特点**：
- 三层独立控制（BGM/GAME_SFX/TARGET_SFX）
- 支持同时播放多层音效
- 基于优先级的音效混音

### 10.3 受惊交互机制

**技术特点**：
- 智能位置同步防止漂移
- 参数化运动基准点反推算法
- 速度渐变产生自然逃离效果

### 10.4 无尽模式动态平衡

**技术特点**：
- 渐进式解锁系统（每500分）
- 属性随机化（每5秒）
- 可配置目标池（全部/已解锁）

### 10.5 惯性滚动算法

**技术特点**：
- 渐进式阻尼（边界高阻尼）
- 平滑吸附算法
- 弹性边界回弹
- 自动轮播机制
